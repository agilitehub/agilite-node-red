'use strict'

const Enums = {
  DEFAULT_NODE_PORT: 80,
  DEFAULT_FLOWS_DIR_NAME: 'flows',
  nodeEnvironment: {
    DEV: 'development',
    TEST: 'testing',
    PROD: 'production'
  }
}

module.exports = Enums
